package stack;

public class Aastackdoucumantation {
	/* 
	 * --------------------------------------------------------------
	 * Stack
	 * --------------
	 * public class Stack<E> extends Vector<E>
	 * 
	 * It is a predefined class available in java.util package. It is the sub class
	 * of Vector class.
	 * 
	 * It is a linear data structure that is used to store the Objects in LIFO (Last
	 * In first out) order.
	 * 
	 * Inserting an element into a Stack is known as push operation where as
	 * extracting an element from the top of the stack is known as pop operation.
	 * 
	 * It throws an unchecked exception called EmptyStackException, if Stack is
	 * empty and we want to fetch the element.
	 * 
	 * It has only one constructor as shown below
	 * 
	 * Stack s = new Stack();
	 */

}
